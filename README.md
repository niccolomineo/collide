A simple collision detection experiment.

It uses an array to represent the user position, walls and walkable space.
Just edit the array following the easy instructions contained in the JS file to create a custom map and have the collision detection automatically adjust. 

See demo [here](http://www.niccolomineo.com/demos/collide/).